/**
 * DTOs for Monzo API endpoints.
 *
 * <p>Contains request and response objects for the Monzo OAuth and connection management APIs.
 * These DTOs ensure that sensitive data (like tokens) is never exposed in API responses.
 */
@NullMarked
package dev.amfshr.budgeteer.api.v1.monzo.dto;

import org.jspecify.annotations.NullMarked;
